// Trong file: lib/definitions.ts

export type LoginData = {
  email: string;
  password: string;
};

export type RegisterData = {
  fullName: string;
  email: string;
  phoneNumber: string;
  password: string;
  role: string;
};
